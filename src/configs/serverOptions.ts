import { customLogger } from "../modules/logger/logger.no-load"

export const serverOpts = {
    // ajv: {
    //     customOptions: {
    //         removeAdditional: 'all'
    //     }
    // },
    logger: {
        level: 'info'
    }
}